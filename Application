package controllers;

import play.*;
import play.mvc.*;

import views.html.*;

public class Application extends Controller {

    public Result start() {
        return ok(start.render());
    }
    public Result date(){
        return ok(start.render());
    }
}
